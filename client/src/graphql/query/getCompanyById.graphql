query GetCompanyById($id: Int!) {
  getCompanyById(id: $id) {
    id
    name
    location
    phone
    logo
    description
    website
    email
    jobs {
      id
      title
      location
      category
      salary
      expDate
      description
      createdAt
      expDate
      imgUrl
    }
  }
}
